/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';

const SenseiProBadge = () => (
	<span className="sensei-pro-badge">{ __( 'Pro', 'sensei-lms' ) }</span>
);

export default SenseiProBadge;
